{
  "name": "@tinycloudlabs/listen-importer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "listen-importer": "./src/cli.ts"
  },
  "scripts": {
    "build": "bun build src/cli.ts --target=bun --outfile dist/listen-importer",
    "check": "tsc --noEmit",
    "test": "bun test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.3.12",
  "dependencies": {
    "@tinycloud/cli": "0.6.0"
  }
}
